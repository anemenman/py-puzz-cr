"""
Дана группа людей состоит из k человек, эти люди могут как-то знать друг друга. Человек является знаменитосью если он
не знает никого а его все знают. Мы можем подходить к каждому и спрашивать знает ли он какогото другого человека.
Нужно определить есть ли знаменитость и кто она используя минимальное колличество вопросов.
"""


def knows(a, b):
    # Заглушка — должна возвращать True, если a знает b, иначе False
    # Реализация зависит от входных данных
    pass


def find_celebrity(k):
    candidate = 0
    for i in range(1, k):
        if knows(candidate, i):
            candidate = i

    # Проверка кандидата
    for j in range(k):
        if j != candidate:
            if knows(candidate, j) or not knows(j, candidate):
                return -1  # знаменитость не найдена

    return candidate

# Пример:
# k = 4
# Реализуйте knows(a, b) и вызовите find_celebrity(k)
